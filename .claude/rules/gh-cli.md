GitHub CLI(`gh`)가 설치되어 있지 않으면 작업 시작 전에 설치한다.

- Windows: `winget install --id GitHub.cli`
- macOS: `brew install gh`
- Linux: https://github.com/cli/cli/blob/trunk/docs/install_linux.md

설치 후 `gh auth login`으로 인증을 완료한다.
